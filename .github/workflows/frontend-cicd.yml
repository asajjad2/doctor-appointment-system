name: Frontend CI/CD

on:
  push:
    paths:
      - "frontend/**"
  pull_request:
    paths:
      - "frontend/**"

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: asajjad2/doctor-appointment-frontend:frontend-${{ github.run_number }}

      - name: Clone the repo
        run: git clone https://github.com/asajjad2/doctor-appointment-system.git temp

      - name: Update docker-compose.yml
        run: |
          cd temp
          sed -i 's|asajjad2/doctor-appointment-frontend|asajjad2/doctor-appointment-frontend:frontend-${{ github.run_number }}|' docker-compose.yml
          git config --global user.name 'asajjad2'
          git config --global user.email 'areebsajjad21@gmail.com'
          git add docker-compose.yml
          git commit -m "Update frontend service to ${{ github.run_number }}"
          git push

      - name: Clean up
        run: rm -rf temp
